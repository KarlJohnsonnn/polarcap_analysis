# Core analysis stack. Use with Python 3.10+.
# On Levante (and other envs where xarray/pandas stack was built for NumPy 1.x),
# pin NumPy to avoid "compiled with NumPy 1.x cannot be run in NumPy 2.x" / _ARRAY_API errors.
numpy>=1.26,<2
